<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>My Blog</title>
<link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
<style>
    .image-slider {
        width: 100%;
        overflow: hidden;
        white-space: nowrap; /* Prevents images from wrapping */
    }

    .slide-track {
        animation: scroll 20s linear infinite; /* Adjust duration for speed */
    }

    .slide {
        display: inline-block;
        margin-right: 20px; /* Spacing between images */
    }

    @keyframes scroll {
        0% { transform: translateX(0); }
        100% { transform: translateX(calc(-100% - 20px)); } /* Adjust 20px for margin */
    }

    .blog-content {
        padding: 20px;
    }
    img{
      max-width: 300px;
      height: auto;
    }
    .innerImages{  /* Style for ALL images in .blog-content */
max-width: 100%;
height: auto;
display: block;
margin: 0 auto;
}


</style>